{
  "openapi": "3.0.0",
  "info": {
    "title": "Users Management API",
    "description": "API for users management. Create, list, update, show user profile and turn users into admins",
    "version": "0.4.0"
  },
  "paths": {
    "/users": {
      "$ref": "./paths/users/root.json"
    },
    "/users/{user_id}/admin": {
      "$ref": "./paths/users/{user_id}.admin.json"
    },
    "/users/{user_id}": {
      "$ref": "./paths/users/{user_id}.json"
    }
  }
}